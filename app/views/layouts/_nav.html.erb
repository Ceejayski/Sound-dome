<nav class="d-flex justify-content-between align-items-center navbar-light">
    <div>
        <%= link_to root_path do %>
            <%= image_tag 'logo.png', class: "navbar-brand", alt: 'logo'%>
        <% end %>
    
    </div>
    <div class="d-flex justify-content-center">
        <div class="nav-item"> <%= active_link_to "HOME", articles_path , class:"nav-link"%> </div>
        <% Category.all.each do |cate| %>
            <div class="nav-item"> <%= active_link_to cate.name.upcase, category_path(cate) , class:"nav-link"%> </div>
        <% end %>
    </div>
    <div class="d-flex justify-content-center">
        <% nav_item.each do |link|%>
            <div class="nav-item"><%= link %></div>
        <% end %> 
    </div>
</nav>